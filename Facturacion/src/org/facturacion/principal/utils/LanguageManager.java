package org.facturacion.principal.utils;

import java.util.ArrayList;
import java.util.List;
import java.util.Locale;
import java.util.ResourceBundle;

public class LanguageManager {
  /*  private static Locale currentLocale = Locale.of("en"); // Idioma por defecto
    private static final String BASE_PATH = "resources.languages.";
    private static final List<Runnable> listeners = new ArrayList<>();

    // Cambiar el idioma y notificar a los observadores
    public static void setLanguage(String languageCode) {
        currentLocale = Locale.of(languageCode);
        notifyListeners();
    }

    // Obtener el bundle de una ventana
    public static ResourceBundle getBundle(String windowName) {
        return ResourceBundle.getBundle(BASE_PATH + windowName, currentLocale);
    }

    // Agregar un listener
    public static void addLanguageChangeListener(Runnable listener) {
        listeners.add(listener);
    }

    // Notificar a todos los listeners
    private static void notifyListeners() {
        for (Runnable listener : listeners) {
            listener.run();
        }
    }*/
}
